
[2026-02-19 15:29:07.882251] QUESTION: how many marketplace courses ongoing?
SQL: SELECT COUNT(*) AS ongoing_marketplace_courses
FROM courses
WHERE course_type = 5
  AND status = 1
LIMIT 1;
ERROR: An error occurred while executing the query. Please try a simpler question.

[2026-02-19 15:35:05.448900] QUESTION: what are the courses are going in Dr Mahalingam College of Engineering and Technology?
SQL: SELECT DISTINCT
    c.id,
    c.course_code,
    c.course_name,
    c.course_type,
    c.description,
    COUNT(DISTINCT cws.id) AS sections_count
FROM courses c
LEFT JOIN course_academic_maps cam ON c.id = cam.course_id
LEFT JOIN course_wise_segregations cws ON c.id = cws.course_id
WHERE cam.college_id = (
    SELECT id FROM colleges
    WHERE college_name LIKE '%Dr Mahalingam College of Engineering and Technology%'
    LIMIT 1
)
GROUP BY c.id, c.course_code, c.course_name, c.course_type, c.description
ORDER BY c.course_name
LIMIT 100;
ERROR: An error occurred while executing the query. Please try a simpler question.

[2026-02-19 15:35:41.077700] QUESTION: what are the courses are going in mcet?
SQL: SELECT DISTINCT
    c.id,
    c.course_code,
    c.course_name,
    c.course_type,
    c.description,
    COUNT(DISTINCT cws.id) AS sections_count
FROM courses c
LEFT JOIN course_academic_maps cam ON c.id = cam.course_id
LEFT JOIN course_wise_segregations cws ON c.id = cws.course_id
WHERE cam.college_id = (SELECT id FROM colleges WHERE college_name LIKE '%mcet%' LIMIT 1)
GROUP BY c.id, c.course_code, c.course_name, c.course_type, c.description
ORDER BY c.course_name
LIMIT 100;
ERROR: An error occurred while executing the query. Please try a simpler question.

[2026-02-19 15:42:25.772905] QUESTION: what are the courses are going in mcet?
SQL: SELECT DISTINCT
    c.id,
    c.course_code,
    c.course_name,
    c.course_type,
    c.description,
    COUNT(DISTINCT cws.id) AS sections_count
FROM courses c
LEFT JOIN course_academic_maps cam ON c.id = cam.course_id
LEFT JOIN course_wise_segregations cws ON c.id = cws.course_id
WHERE cam.college_id = (SELECT id FROM colleges WHERE college_name LIKE '%mcet%' LIMIT 1)
GROUP BY c.id, c.course_code, c.course_name, c.course_type, c.description
ORDER BY c.course_name
LIMIT 100;
ERROR: An error occurred while executing the query. Please try a simpler question.

[2026-02-19 15:42:46.039502] QUESTION: what are the courses are going in mcet?
SQL: SELECT DISTINCT
    c.id,
    c.course_code,
    c.course_name,
    c.course_type,
    c.description,
    COUNT(DISTINCT cws.id) AS sections_count
FROM courses c
LEFT JOIN course_academic_maps cam ON c.id = cam.course_id
LEFT JOIN course_wise_segregations cws ON c.id = cws.course_id
WHERE cam.college_id = (SELECT id FROM colleges WHERE college_name LIKE '%mcet%' LIMIT 1)
GROUP BY c.id, c.course_code, c.course_name, c.course_type, c.description
ORDER BY c.course_name
LIMIT 100;
ERROR: An error occurred while executing the query. Please try a simpler question.

[2026-02-19 15:43:50.547111] QUESTION: what are the courses are going in mcet?
SQL: SELECT DISTINCT
    c.id,
    c.course_code,
    c.course_name,
    c.course_type,
    c.description,
    COUNT(DISTINCT cws.id) AS sections_count
FROM courses c
LEFT JOIN course_academic_maps cam ON c.id = cam.course_id
LEFT JOIN course_wise_segregations cws ON c.id = cws.course_id
WHERE cam.college_id = (SELECT id FROM colleges WHERE college_name LIKE '%mcet%' LIMIT 1)
GROUP BY c.id, c.course_code, c.course_name, c.course_type, c.description
ORDER BY c.course_name
LIMIT 100;
ERROR: An error occurred while executing the query. Please try a simpler question.
